# Virtual Memory Concepts
Demand paging loads pages when needed. Page tables map virtual page numbers to physical frames. The TLB caches recent translations. Excessive page faults can cause thrashing. Working-set and page-fault frequency ideas are used to reason about memory pressure.
