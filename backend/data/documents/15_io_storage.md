# I/O and Storage
Device drivers abstract hardware. Interrupts allow devices to notify the CPU, while DMA can transfer blocks of data between devices and memory with less CPU involvement. Buffers and caches can improve throughput, while storage layers manage blocks and persistence.
