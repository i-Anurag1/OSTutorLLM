# Threads
A thread is a schedulable execution unit within a process. Threads in the same process share address space and many resources. User-level threads are managed by a library, while kernel-level threads are managed by the operating system. Thread lifecycle states can be modeled as NEW, READY, RUNNING, BLOCKED, and TERMINATED.
