# File Systems
A file system organizes persistent data into files and directories and manages metadata, allocation, protection, and naming. Typical concepts include inodes or file control blocks, directory entries, contiguous allocation, linked allocation, indexed allocation, permissions, and journaling.
