# Memory Management
Virtual memory gives processes the abstraction of a large logical address space. Paging divides virtual memory into fixed-size pages and physical memory into fixed-size frames. A page fault occurs when a referenced page is not resident. Segmentation organizes an address space into logical segments with base and limit values.
