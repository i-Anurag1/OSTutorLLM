# Segmentation
A logical address can be represented as a segment number and offset. Each segment has a base and limit. The address is valid when the offset is less than the segment limit, and physical address is base plus offset.
