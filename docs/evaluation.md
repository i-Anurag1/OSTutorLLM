# Evaluation Matrix

Core static checks included in the package:

- Python compilation: `python -m compileall -q backend/app`
- Backend simulator suite: `pytest -q backend/tests`
- End-to-end local component smoke: `python scripts/self_test.py`

Manual browser acceptance path:

Overview -> AI Tutor -> Topics -> Simulators -> Quiz Lab -> Code Labs -> Analytics -> Knowledge Base (faculty/admin).

Default evaluation accounts are listed in START-HERE.txt.
